# 数据与日志（仅根目录 data/；invest/data 是代码，必须提交）
/data/
logs/
*.db
*.db-wal
*.db-shm

# 环境与密钥
.env
.env.backup*

# 本地临时/测试产物
.tmp/
_pytest_out.txt
probe_verify_tmp.py

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# 系统
Thumbs.db
.DS_Store

# 虚拟环境
myenv/

# external vendor repos (independent git repos; keep out of main repo)
tools/

# .claude/skills：只保留本系统自沉淀 skill（trader-system/），
# 其余为外部 git 仓库/方法论素材（独立管理，不入主仓库）
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/trader-system/
=  # 0-byte accidental empty file
=
.dsh/
_piptmp/
